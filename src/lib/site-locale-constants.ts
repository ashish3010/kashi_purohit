/** Cookie + localStorage key — keep in sync with `LanguageSwitcher`. */
export const SITE_LOCALE_STORAGE_KEY = "kashi-locale";

export const SITE_LOCALE_COOKIE = "kashi-locale";

export type SiteLocale = "en" | "hi";
